# 2026-07-08 仓库清理记录

本次清理将仓库从排障工作目录收敛为可维护基线：

- 保留 `patched-8` 稳定回退包和 `patched-17` 当前成功包。
- 删除 patched-2、5、6、7、9 至 16 等失败或过渡包及其专用入口。
- 删除大量重复 Xorg/DDX/probe 原始日志，只保留精简 pass/fail 基线。
- 外部 deb 改为 release 独立下载并由 `.gitignore` 排除。
- Deepin deb 默认从仓库根目录读取，解包到被忽略的 `third_party/`。
- 仓库和用户路径改为 `$INNOGPU_ROOT`、`$INNOGPU_X_USER`、`$INNOGPU_X_HOME` 等可覆盖变量。

后续维护从 `patched-17` 继续，`patched-8` 只用于恢复，不再修改。
