# Cleanup Notes 2026-07-08

## Goal

整理仓库，使它能作为后续维护起点，而不是保留排障过程中的临时堆积。

本次清理遵循三条规则：

- 保留可回退的稳定包：`patched-8`。
- 保留当前成功包：`patched-17`。
- 代码和文档不再写死本机路径，统一使用 `$HOME`、`$INNOGPU_ROOT`、`$INNOGPU_DEEPIN_ROOT`、`$INNOGPU_X_USER`、`$INNOGPU_X_HOME`。

## Removed

删除内容：

- 临时日志文件 `111`。
- 中间 `.deb` 包：`patched-2`、`patched-5`、`patched-6`、`patched-7`、`patched-9` 到 `patched-16`。
- 依赖已删除中间包的旧构建/安装入口：`build-patched6/7/9`、`build-patched11` 到 `build-patched16`、`install-patched12` 到 `install-patched16`。
- 大量原始 `baselines` Xorg/DDX/probe 日志。

删除原因：

- 中间包包含失败或过渡状态，继续保留容易误装。
- 原始日志体积大、重复、包含主机私有路径。
- 最终判断只需要保留明确的 pass/fail 基线和回退点。

## Kept

保留内容：

- `innogpu-fh2m-trixie_3.3.3.42-patched-8.deb`：原始稳定回退点。
- `innogpu-fh2m-trixie_3.3.3.42-patched-17.deb`：当前成功点。
- `patches/001` 到 `patches/006`：构建 Deepin 基线和本设备显示修复所需补丁。
- `scripts/build-patched17-deepin-local-display.sh` 和相关安装/验证脚本。
- 精简后的 `baselines/final-summary.md` 和最新结果文件。

## Path Cleanup

处理方式：

- 仓库根目录由脚本自动推导：`$(dirname "${BASH_SOURCE[0]}")/..`。
- Deepin 原始 `.deb` 默认从仓库根目录读取，解包目录默认值为 `$INNOGPU_ROOT/third_party/innogpu-fh2m-deepin-202504/root`，可用 `INNOGPU_DEEPIN_DEB` 和 `INNOGPU_DEEPIN_ROOT` 覆盖。
- Xorg 登录用户默认从 `INNOGPU_X_USER`、`SUDO_USER`、`USER` 推导。
- X 用户 home 默认从 `getent passwd` 推导，可用 `INNOGPU_X_HOME` 覆盖。

## Current Maintenance Baseline

后续工作应从 `patched-17` 开始，不再回到 `patched-10` 到 `patched-16` 的中间状态。

如果需要排查新问题：

```bash
cd "$INNOGPU_ROOT"
scripts/check-innogpu-progress.sh
scripts/check-desktop-hwgl.sh
```

如果新改动失败：

```bash
cd "$INNOGPU_ROOT"
sudo dpkg -i innogpu-fh2m-trixie_3.3.3.42-patched-8.deb
sudo scripts/disable-incompatible-userspace.sh
sudo reboot
```
